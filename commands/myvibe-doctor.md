---
description: Verify the myvibe environment — ffmpeg, hevc_videotoolbox, Proxima Nova font, and claude-video-vision plugin.
---

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/doctor.sh"
```

If anything reports missing, run `/myvibe-setup` to install it.
